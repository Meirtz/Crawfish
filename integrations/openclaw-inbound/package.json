{
  "name": "@crawfish/openclaw-inbound",
  "version": "0.1.0-alpha.1",
  "private": true,
  "type": "module",
  "description": "Thin OpenClaw inbound Gateway RPC bridge for Crawfish.",
  "bin": {
    "openclaw-crawfish-inbound": "./dist/src/bin/openclaw-crawfish-inbound.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/tests/*.test.js"
  },
  "devDependencies": {
    "@types/node": "^22.15.30",
    "typescript": "^5.8.3"
  }
}
